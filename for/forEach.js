const setup = [
    'monitor',
    'teclado',
    'mouse',
    'cpu',
    'gabinete',
    'processador',
    'placaMae'
]

setup.forEach((value, index) => {
    console.log(`${index +1}_ ${value}`)
})